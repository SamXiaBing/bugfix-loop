# 贡献指南

欢迎来改。这个 skill 的价值在基本原则和经验库，不在代码行数。

## 能贡献什么

1. 新的项目类型适配包。照 `references/pack-authoring.md` 的写法，在 `references/project-types/` 下建你的类型，配一个真实的分类法和检查顺序。
2. 新的调试基本原则。先问自己这条原则是不是跟项目类型无关的，有没有真实例子托着。
3. 脚本修复。scripts/ 里的脚本跑挂了，修好并补进冒烟测试。
4. 文档和措辞。发现说人话不到位的地方，直接改。

## 验收门槛

- 中文文档过 `python tools/check_prose.py`，零失败。
- 脚本改动跑通 `python scripts/smoke_test.py`。
- 不出现任何真实公司或工程的专属词，专属概念只能以匿名例子出现。
- 每一条新纪律都能回答我具体要做什么动作。

## 怎么提

1. 先开 issue 说你想改什么，等确认再动手，避免白干。
2. 改动附带自测记录，写进 `tests/run-XXX.md`。
3. PR 描述里写清楚改了哪几个文件、对应哪个阶段。

## 红线

- 不出现真实工具名、仓库名、域名、团队名、字段名。
- 凭证不写进任何文件，用环境变量。
